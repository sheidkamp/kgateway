apiVersion: v1
kind: Secret
metadata:
  name: headermods-backend-creds
  namespace: kgateway-base
type: Opaque
stringData:
  api-key: my-secret-api-key
  tenant-id: tenant-abc
---
apiVersion: gateway.kgateway.dev/v1alpha1
kind: TrafficPolicy
metadata:
  name: headermods-from-secret-policy
  namespace: kgateway-base
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: headermods-route-1
  headerModifiers:
    request:
      set:
      - name: X-Api-Key
        secretRef:
          name: headermods-backend-creds
          key: api-key
          namespace: kgateway-base
      - name: X-Tenant-Id
        secretRef:
          name: headermods-backend-creds
          key: tenant-id
          namespace: kgateway-base
      remove:
      - X-Request-Id
    response:
      remove:
      - X-Envoy-Upstream-Service-Time
