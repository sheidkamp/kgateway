apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: example-gateway
spec:
  gatewayClassName: example-gateway-class
  listeners:
    - name: http
      protocol: HTTP
      port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: example-route
spec:
  parentRefs:
    - name: example-gateway
  hostnames:
    - "example.com"
  rules:
    - backendRefs:
        - name: example-svc
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /foo
    - backendRefs:
        - name: example-svc
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /bar
---
apiVersion: v1
kind: Service
metadata:
  name: example-svc
spec:
  selector:
    test: test
  ports:
    - protocol: TCP
      port: 80
      targetPort: test
---
apiVersion: gateway.kgateway.dev/v1alpha1
kind: TrafficPolicy
metadata:
  name: gateway-test
spec:
  targetRefs:
    - kind: Gateway
      group: gateway.networking.k8s.io
      name: example-gateway
  acl:
    defaultAction: allow
    rules:
      - name: block-internal-range
        cidrs:
          - "10.0.0.0/8"
        action: deny
      - name: allow-trusted-subnet
        cidrs:
          - "10.1.0.0/16"
        action: allow
      - name: block-ipv6-range
        cidrs:
          - "2001:db8::/32"
        action: deny
      - name: block-single-ipv4
        cidrs:
          - "192.168.1.100"
        action: deny
      - name: block-single-ipv6
        cidrs:
          - "2001:db8::1"
        action: deny
    denyResponse:
      statusCode: 403
      blockedByHeaderName: X-Blocked-By
